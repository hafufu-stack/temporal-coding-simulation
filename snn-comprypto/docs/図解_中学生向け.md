# SNN Comprypto 図解（中学生向け）

## Zennに貼れるMermaid図（シンプル版）

```mermaid
flowchart LR
    subgraph 送り手
        A[データ 100] --> B[差を計算 +2]
        C[🧠脳] --> B
        C --> D[合言葉 55]
        B --> E[暗号化]
        D --> E
        E --> F[暗号 53]
    end
    
    F --> G[ネット]
    
    subgraph 受け手
        G --> H[暗号 53]
        I[🧠同じ脳] --> J[同じ合言葉 55]
        H --> K[解読]
        J --> K
        K --> L[差 +2]
        I --> M[予測 98]
        L --> N[復元]
        M --> N
        N --> O[データ 100]
    end
```

## もっとシンプルな図（縦版）

```mermaid
flowchart TD
    A[🔵 元のデータ: 100] --> B[🧠 AIが予測: 98]
    B --> C[📉 差だけ取り出す: +2]
    C --> D[🔐 合言葉で暗号化: 53]
    D --> E[📡 送信]
    E --> F[🔓 同じ合言葉で解読: +2]
    F --> G[🧠 同じAIが同じ予測: 98]
    G --> H[✅ 予測+差 = 100]
```

## 超簡単な説明

### 🧠 双子の脳みそ作戦

1. **送る人と受け取る人が、同じ「脳みそのコピー」を持っている**
   - 二人とも同じAI（SNN）を持ってる
   - 同じ入力を見ると、同じ反応をする

2. **脳みそが「次はこれだろうな」と予測する**
   - 例：「次のデータは98だろう」と予測
   - 実際のデータは100だった

3. **「予測」と「実際」の差だけを送る**
   - 100 - 98 = 2
   - 100を送るより2を送る方がコンパクト！（これが圧縮）

4. **その差を「今日だけの合言葉」で隠す**
   - 脳みその状態から合言葉を作る
   - 合言葉は毎回変わるから安全！（これが暗号化）

5. **受け取る人も同じ脳みそを持ってるから…**
   - 同じ予測ができる（98）
   - 同じ合言葉を知ってる（55）
   - だから元のデータ（100）を復元できる！

## 一言でいうと

> **「双子のAIだけが知ってる合言葉」で秘密の手紙を送る仕組み**
